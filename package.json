{
  "name": "fuelpro-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "FuelPro - Fuel Station Management System",
  "scripts": {
    "test": "echo \"Tests run in app/ directory\" && exit 0",
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "check": "cd app && npm run check",
    "format": "cd app && npm run format"
  },
  "keywords": ["fuel", "station", "management", "pos"],
  "author": "FuelPro",
  "license": "ISC",
  "devDependencies": {
    "vercel": "^54.17.3"
  }
}
